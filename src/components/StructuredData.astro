---
export interface Props {
  type: "BlogPosting" | "Person" | "WebSite";
  data: any;
}

const { type, data } = Astro.props;

let structuredData = {};

if (type === "BlogPosting") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    headline: data.title,
    description: data.description,
    author: {
      "@type": "Person",
      name: data.author || "Abhi",
      url: "https://abhieq31.com/about",
    },
    datePublished: data.pubDatetime.toISOString(),
    dateModified: data.modDatetime?.toISOString() || data.pubDatetime.toISOString(),
    publisher: {
      "@type": "Person",
      name: "Abhi",
      logo: {
        "@type": "ImageObject",
        url: "https://abhieq31.com/peter-avatar.jpg",
      },
    },
    mainEntityOfPage: {
      "@type": "WebPage",
      "@id": data.url,
    },
    image: data.ogImage || "https://abhieq31.com/peter-avatar.jpg",
    articleSection: data.tags?.[0] || "Technology",
    keywords: data.tags?.join(", ") || "",
    wordCount: data.wordCount,
    timeRequired: data.readingTime,
  };
} else if (type === "Person") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "Person",
    name: "Abhi",
    url: "https://abhieq31.com",
    image: "https://abhieq31.com/peter-avatar.jpg",
    sameAs: [
      "https://x.com/abhieq31",
      "https://instagram.com/abhieq31",
      "https://github.com/abhieq31",
    ],
    jobTitle: "Software Engineer",
    description:
      "AI-powered tools from Swift roots to web frontiers. Every commit lands on GitHub for you to fork & remix.",
  };
} else if (type === "WebSite") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: "Abhi",
    url: "https://abhieq31.com",
    description:
      "AI-powered tools from Swift roots to web frontiers. Every commit lands on GitHub for you to fork & remix.",
    author: {
      "@type": "Person",
      name: "Abhi",
    },
    potentialAction: {
      "@type": "SearchAction",
      target: "https://abhieq31.com/search?q={search_term_string}",
      "query-input": "required name=search_term_string",
    },
  };
}
---

<script type="application/ld+json" set:html={JSON.stringify(structuredData)} />